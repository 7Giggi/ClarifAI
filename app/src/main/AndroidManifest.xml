<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permessi -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Feature camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClarifAI"
        tools:targetApi="31">

        <!-- META-DATA CRITICO: Download automatico modelli ML Kit -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr,image_labeling,object_detection" />

        <!-- Activity principale -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Modalità 1: Descrizione Continua -->
        <activity
            android:name=".ui.descContinua.DescrizioneContinuaActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Modalità 2: Lettura Testo -->
        <activity
            android:name=".ui.lettura.letturaActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Modalità 3: Rilevamento Ostacoli -->
        <activity
            android:name=".ui.ostacoli.ostacoliActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

    </application>

</manifest>


